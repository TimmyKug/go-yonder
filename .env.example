# The app defaults to official OpenStreetMap raster tiles when this is unset.
# Set this only to override the complete MapLibre style. EXPO_PUBLIC_ values are
# bundled into the client and must not contain secrets.
# EXPO_PUBLIC_MAP_STYLE_URL=https://tiles.example.com/style.json
