# Development defaults to MapLibre's public demo style when this is unset.
# EXPO_PUBLIC_ values are bundled into the client and must not contain secrets.
EXPO_PUBLIC_MAP_STYLE_URL=https://demotiles.maplibre.org/style.json
